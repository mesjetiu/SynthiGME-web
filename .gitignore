.release-cache/
/local/
node_modules/

# Claude Code
# CLAUDE.md SÍ va al repo desde el 21/09/2026: no tiene nada privado, y así
# llega solo a todas las máquinas. Lo propio de cada máquina va en el .local.
CLAUDE.local.md
CLAUDE.pc-*.md
.commit_msg.txt
# Copias de trabajo de las sesiones. En el servidor el servicio arranca con
# `--spawn worktree`: cada conversación se crea un checkout del repo aquí
# dentro. Es entorno, no contenido.
.claude/

# Estudio técnico-histórico (repositorio separado)
synthiGME-implementation-study/

# Logs
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# OS / editor files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Env files (keep example)
.env
.env.*
!.env.example

# Common caches
.cache/
.parcel-cache/
.turbo/
coverage/

# Build outputs
# ⚠️ IMPORTANTE: docs/ se INCLUYE en los commits porque se sirve desde GitHub Pages
# Los otros directorios de build se excluyen porque se regeneran en cada compilación
dist/
build/
out/

# Electron build outputs (siempre excluir, se regeneran)
dist-electron/
dist-app/

# TypeScript / tooling artifacts
*.tsbuildinfo
.eslintcache
*.tgz
test-results/
commit-msg.txt

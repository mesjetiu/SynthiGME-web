test(state): añadir tests exhaustivos para UndoRedoManager

test(osc): añadir tests para oscMatrixSync (direcciones, valores, colores, convenciones)
test(osc): añadir tests de contratos y deduplicación para módulos OSC sync (InputAmp, OutputChannel, NoiseGenerator, Joystick)
test(ui): añadir tests para RecordingOverlay (temporizador, clases, eventos)
test(core): añadir tests de formatos de grabación WAV/WebM/Opus en RecordingEngine

docs(oscAddressMap): actualizar tests para nuevos módulos audio, cv, joy y eliminar patchA/patchV obsoletos
fix(state): corregir off-by-one en test FIFO de UndoRedoManager
fix(osc): corregir test de módulos principales eliminando patchA/patchV

- Cobertura completa de features recientes: undo/redo, matrices OSC, sync de módulos, overlay REC, formatos WebM/Opus
- Todos los tests pasan (2221/2221)
- No se han ejecutado tests de audio (test:all)
